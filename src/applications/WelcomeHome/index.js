
namespace `applications` (
    class WelcomeHome extends w3c.ui.Application {
        constructor(element){
            super(element);
        }

        onConnected() {
            super.onConnected();

        }
    }
);
