
namespace `applications` (
    class MyTest extends w3c.ui.Application {
        constructor(element){
            super(element);
        }

        onConnected() {
            super.onConnected();

        }
    }
);
