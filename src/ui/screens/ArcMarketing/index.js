namespace `ui.screens` (
    class ArcMarketing extends Application {
        async onConnected() {
            await super.onConnected();
            console.log("ArcMarketing demo booted");
        }
    }
);
