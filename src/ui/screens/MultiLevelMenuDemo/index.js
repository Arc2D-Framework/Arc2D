import 'ui.components.Splash';
import 'ui.components.MultiLevelMenu';

namespace `ui.screens` (
    class MultiLevelMenuDemo extends Application {
        async onConnected(){
            await super.onConnected();
        }
    }
);
