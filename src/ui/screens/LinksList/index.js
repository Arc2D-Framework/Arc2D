
namespace `ui.screens` (
    class LinksList extends Application {
        async onConnected(){
            await super.onConnected()
            // var c = new ui.components.RippleButton();
            //     c.assignSlot(`<span slot="label">Hello There</span>`.toNode());
            // await this.append(c);
        }
    }
);
