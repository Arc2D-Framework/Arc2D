@tag("contact-page");
namespace `display.views` (
	class Contact extends w3c.ui.WebComponent  {
		constructor(){
            super();
		}
		
		async onConnected() {
			await super.onConnected();
		}
	}
)