namespace `system.input` (
    class Mouse {
        static initialize(){
            document.addEventListener("mousedown", (e) => {
               //TODO
            })
        }
    }
);

system.input.Mouse.initialize();
Mouse = system.input.Mouse;