import 'core.ui.ProjectExplorer';

namespace `applications` (
    class ProjectExplorerDemo extends w3c.ui.Application {
        constructor(element){
            super(element);
        }

        async onConnected() {
            await super.onConnected();
        }
    }
);
