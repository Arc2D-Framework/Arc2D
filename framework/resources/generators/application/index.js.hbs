
namespace `{{FQNS_to_NS namespace}}` (
    class {{FQNS_to_Class namespace}} extends w3c.ui.Application {
        constructor(element){
            super(element);
        }

        onConnected() {
            super.onConnected();

        }
    }
);
