
namespace `{{namespace}}` (
    class {{class}} extends w3c.ui.Application {
        constructor(element){
            super(element);
        }

        onConnected() {
            super.onConnected();

        }
    }
);
